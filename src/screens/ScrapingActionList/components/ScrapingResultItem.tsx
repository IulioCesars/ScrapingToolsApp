import React from "react";
import { Text, View } from "react-native";

export function ScrapingResultItem(props: { value: any }) {
    const { value } = props;

    return (
        <View>
            <Text> ScrapingActionResultItem </Text>
        </View>
    )
}